import { Meteor } from 'meteor/meteor';
import '../imports/api/productos.js';

//Llamada al servidor para que se inicie y pueda reflejarse en la pagina web

Meteor.startup(() => {
  // code to run on server at startup
});
